// import { IUser, IUserListContext } from '../types'

// export const userListHelper = (
//   response: { user: IUser; uid: string },
//   userListContext: IUserListContext
// ) => {
//   const { uid, user } = response
//   const userList = structuredClone(userListContext)
//   userList[uid] = user
//   return userList
// }
